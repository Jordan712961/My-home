import { Typography, Box } from '@mui/material';

const GoalTracker = () => (
  <Box>
    <Typography variant="h4" gutterBottom>Goal Tracker</Typography>
    <Typography>Goal tracking implementation coming soon...</Typography>
  </Box>
);

export default GoalTracker;
