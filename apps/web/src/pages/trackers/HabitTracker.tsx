import { Typography, Box } from '@mui/material';

const HabitTracker = () => (
  <Box>
    <Typography variant="h4" gutterBottom>Habit Tracker</Typography>
    <Typography>Habit tracking implementation coming soon...</Typography>
  </Box>
);

export default HabitTracker;
