import { Typography, Box } from '@mui/material';

const SymptomTracker = () => (
  <Box>
    <Typography variant="h4" gutterBottom>Symptom Tracker</Typography>
    <Typography>Symptom tracking implementation coming soon...</Typography>
  </Box>
);

export default SymptomTracker;
